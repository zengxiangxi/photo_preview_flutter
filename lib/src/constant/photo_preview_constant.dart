import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class PhotoPreviewConstant{
    ///默认初始页
    static const int DEFAULT_INIT_PAGE = 0;

    ///默认背景色
    static const Color DEFAULT_BACK_GROUND_COLOR = Colors.black;

    ///默认当前缩放倍数
    static const double DEFAULT_TOTAL_SCALE = 1.0;

    ///默认初始化缩放倍数
    static const double DEFAULT_INIT_SCALE = 1.0;

    ///重置回弹时间
    static const int DEFAULT_RESET_MILL = 200;

    ///双击放大缩小默认值
    static const List<double> DEFAULT_DOUBLE_TAP_SCALE = [1.0, 2.0];

    ///默认最小缩放大小
    static const double DEFAULT_MIX_SCALE = 1.0;

    ///默认最大缩放大小
    static const double DEFAULT_MAX_SCALE = 5.0;

}